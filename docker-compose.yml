services:
  app:
    container_name: zavmo_app
    build: .
    ports:
      - "8000:8000"
    expose:
      - "8000"
    volumes:
      - .:/app
    env_file:
      - .env  
    restart: unless-stopped
    networks:
      - zavmonetwork

networks:
  zavmonetwork:
    external: true
